import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const Markets: React.FC = () => {
  return (
    <Container maxWidth="lg">
      <Typography variant="h4">
        Marketler
      </Typography>
      <Box>
        {/* Market listesi buraya gelecek */}
      </Box>
    </Container>
  );
};

export default Markets; 